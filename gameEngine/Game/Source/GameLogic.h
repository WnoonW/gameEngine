#pragma once

// Game project only — add gameplay C++ here (not in Engine/Editor).
// Later this can register systems, player controllers, etc. with Engine.
namespace GameLogic
{
    void OnProcessStart();
    void OnProcessExit();
}
